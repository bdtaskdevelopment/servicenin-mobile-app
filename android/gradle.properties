org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Project and Pub cache live on different drive letters (E: vs C:) on this machine.
# Kotlin's incremental compiler crashes trying to relativize paths across drive roots on Windows.
kotlin.incremental=false
