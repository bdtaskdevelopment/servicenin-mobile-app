# ServiceNin environment config
# Replace the placeholder URLs/keys with your real values.

BASE_URL=http://192.168.1.155:8080/
IMAGE_URL=http://192.168.1.155:8080/images/

#BASE_URL=https://demo.servicenin.net/
#IMAGE_URL=https://demo.servicenin.net/images/

# Payment gateway keys (leave empty if unused)https://demo.servicenin.net
stripePublishableKey=
stripeSecretKey=
paypalClientKey=
paypalSecretKey=

# Google Places + Directions API key is no longer read from here — it's
# fetched at runtime from GET /api/v1/settings (field "map_api_key"),
# configured server-side under Admin > Settings > Map.

# for now focus on blood bank front page there have register donner card you just comment it and add a button for available for donate give a switch button or you can adjust by this so fix it